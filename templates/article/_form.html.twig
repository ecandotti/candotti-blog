{% if app.user and is_granted('ROLE_ADMIN') %}
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mr-2 mb-2">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }}
{% endif %}


