{% extends 'base.html.twig' %}

{% block title %}Editer l'article{% endblock %}

{% block body %}
{% if app.user and is_granted('ROLE_ADMIN') %}
    <a href="{{ path('home') }}"><h3>🏠</h3></a>
    <h1 class="text-center mb-2">Editer l'article</h1>
    
    {{ include('article/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

    {{ include('article/_delete_form.html.twig') }}
{% else %}
    <a href="{{ path('home') }}"><h3>🏠</h3></a>
    <div>
        Vous n'avez pas accès à cet page
    </div>
{% endif %}
{% endblock %}
