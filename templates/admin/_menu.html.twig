<div class="mb-4">
    <div class="d-flex justify-content-center">
        <a href="{{ path('admin_dashboard') }}" class="btn btn-secondary mr-4 mb-4">Tableau de board</a>
        <a href="{{ path('admin_manage_article') }}" class="btn btn-secondary mr-4 mb-4">Gérer mes articles</a>
        <a href="{{ path('admin_manage_comment') }}" class="btn btn-secondary mr-4 mb-4">Gérer les commentaires</a>
        <a href="{{ path('article_new') }}" class="btn btn-success mr-4 mb-4"><i class="fas fa-plus-square"></i></a>
    </div>
    <div class="d-flex justify-content-center">
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Rechercher quelque chose">
            <button class="btn btn-secondary my-2 my-sm-0" type="submit">Go!</button>
        </form>
    </div>
</div>